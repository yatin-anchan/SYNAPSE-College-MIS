<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/splash_card_bg" />

    <!-- Corner radius for rounded edges -->
    <corners android:radius="12dp" />

    <!-- Optional stroke/border -->
    <stroke
        android:width="1dp"
        android:color="@color/splash_card_stroke" />

    <!-- Optional padding -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp" />

</shape>